from typing import Any


class CandleBuilder:
    """Placeholder for converting ticks into candles in a later market-data phase."""

    def build(self, ticks: list[dict[str, Any]]) -> list[dict[str, Any]]:
        return []

